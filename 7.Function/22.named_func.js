// Named Function
function namedFunction(a, b) {
    // This is a named function that takes two parameters 'a' and 'b'
    console.log("Named Function result: " + (a + b)); // It adds 'a' and 'b' and logs the result
}

namedFunction(5, 10); // Call the named function with arguments 5 and 10
